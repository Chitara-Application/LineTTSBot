{
  "main": "dist/gui/main.cjs",
  "scripts": {
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-gui-static.mjs",
    "gui": "npm run build && electron ."
  }
}